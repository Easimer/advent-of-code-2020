switch("threads", "on")
switch("opt", "speed")
switch("d", "release")

switch("debugger", "native")
switch("checks", "off")
switch("stackTrace", "off")
switch("lineTrace", "off")
switch("threadAnalysis", "off")
switch("implicitStatic", "on")

#switch("stackTrace", "on")
#switch("profiler", "on")
